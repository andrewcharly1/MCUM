interface:
  display_name: "MCUM Orchestrator"
  short_description: "Workspace orchestrator for projects, memory, and execution logging"
  brand_color: "#2563EB"
  default_prompt: "Use $mcum-orchestrator as the outer orchestration layer for any project task in this workspace, including analysis, fixes, tests, scraping, and memory logging."

policy:
  allow_implicit_invocation: true
